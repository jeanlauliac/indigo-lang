'Push item on vector':
  stdout: ''
  files:
    index.clv: >
      fn main() {
        let v = vec<u32> [];
        push(&v, 10);
        if (v[0] != 10) __die("fail");
      }

'Push items on vector':
  stdout: ''
  files:
    index.clv: >
      fn main() {
        let v = vec<u32> [];
        push(&v, 10);
        push(&v, 20);
        if (v[0] != 10) __die("fail");
        if (v[1] != 20) __die("fail");
      }

